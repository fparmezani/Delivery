{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node",
      "build": "export NODE_OPTIONS=--openssl-legacy-provider; ng build"
    }
  ],
  "outputDirectory": "."
}
